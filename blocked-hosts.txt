# Hosts this instance cannot fetch, beyond what no-watch.json already records.
#
# Most blocked hosts arrive here as a `datacenter-block` or `anti-bot` record in no-watch.json,
# and scripts read the host out of that file. These few have no record because the watches were
# removed before that list existed; the businesses behind them are real and would otherwise be
# proposed again on every pass.
#
# A candidate on one of these hosts looks perfectly healthy from a home connection and turns red
# the moment it runs in the cluster. Measured: 200 from here, 403 from the VPS, same user agent,
# same second.
#
# A block is not forever. re-gruppe.de and re-fd.de stood here until 22.08.2026 and now answer
# the cluster with 200, so six pages came back. Re-measure from inside the pod before believing
# this file: `kubectl -n changedetection exec deploy/changedetection -- python3 -c "import
# requests; ..."` is the only client that counts, because curl from the VPS shell has a
# different TLS fingerprint.
#
# one host per line, reason after the hash
euronics.de          # 405 to the cluster, 2.5 kB, no challenge (22.08.2026)
hotel-bb.com         # 403, 394 bytes (22.08.2026)
luna-fulda.de        # 403, 4.5 kB (22.08.2026)
