__pycache__/
*.pyc
*.tmp
scripts/*.tmp
data/*.tmp

# sync cache: slug -> uuid is derived at reconcile time and only matches one instance
entries/.lock.json
entries/.lock.*.json

# Momentaufnahmen aus cd_export.py — jederzeit neu erzeugbar, nicht die Wahrheit
export/

# lokale Werkzeug-, Editor- und Notizdateien
.idea/
.vscode/
.claude/
CLAUDE.md
*.original.md

# kubectl legt hier seinen Cache an, wenn KUBECONFIG relativ gesetzt wird
.kube/

# Bundles aus dem Umschreiben der Historie, lokal aufbewahrt
.git-history-backup/
